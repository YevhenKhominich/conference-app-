server:
  port: 8090
  servlet:
    context-path: /app
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db?verifyServerCertificate=false&useSSL=false&allowPublicKeyRetrieval=true
    password: pass1
    username: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: false
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true



